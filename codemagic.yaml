workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter packages
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter pub get
      - name: Install CocoaPods dependencies
        script: |
          cd $CM_BUILD_DIR/mobile_app/ios
          pod install
      - name: Build iOS
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter build ios --release --no-codesign
    artifacts:
      - mobile_app/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - mobile_app/flutter_drive.log

  ios-release:
    name: iOS Release (App Store)
    max_build_duration: 90
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      groups:
        - app_store_credentials
    scripts:
      - name: Get Flutter packages
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter pub get
      - name: Install CocoaPods dependencies
        script: |
          cd $CM_BUILD_DIR/mobile_app/ios
          pod install
      - name: Set up code signing
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "com.utap.mobileApp" --type IOS_APP_STORE --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Build iOS IPA
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - mobile_app/build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false

  android-workflow:
    name: Android Build
    max_build_duration: 60
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter packages
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter pub get
      - name: Build APK
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter build apk --release
      - name: Build App Bundle
        script: |
          cd $CM_BUILD_DIR/mobile_app
          flutter build appbundle --release
    artifacts:
      - mobile_app/build/app/outputs/**/*.apk
      - mobile_app/build/app/outputs/**/*.aab
